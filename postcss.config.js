// postcss.config.js

module.exports = {
  plugins: {
    // Activa la compilación de Tailwind CSS
    'tailwindcss': {},
    // Añade prefijos de proveedor para compatibilidad con navegadores
    'autoprefixer': {},
  },
}